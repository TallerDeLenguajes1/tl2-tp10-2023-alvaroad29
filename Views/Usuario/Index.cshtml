@{
    ViewData["Title"] = "Usuarios"; // titulo de la pestaña
}

@* para q reconozca el viewModel *@
@* o asi ----->   @model MVC.ViewModels.ProductoViewModel *@


@using tl2_tp10_2023_alvaroad29.ViewModels 

@model ListaUsuariosViewModel

<section>
    <article>
        <h2 class="text-center">Lista Usuarios</h2>

        <table class="table">
            <thead>
                <tr>
                    <th hidden>ID</th> <!-- oculto el id--->
                    <th>Usuario</th>
                    <th>Rol</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var usuario in Model.UsuariosVM)
                {
                    <tr>
                        <td hidden>@usuario.Id</td>
                        <td>@usuario.NombreDeUsuario</td>
                        <td>@usuario.Rol.ToString()</td>
                        <td>
                                <a asp-controller="Usuario" asp-action="Update" asp-route-id="@usuario.Id" class="btn btn-primary">Modificar</a>

                                <a asp-controller="Usuario" asp-action="Delete" asp-route-id="@usuario.Id" onclick="return confirm('¿Estas seguro que quieres eliminar el elemento?')" class="btn btn-primary">Eliminar</a>  
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <a type="button" class="btn btn-primary btn-lg" asp-controller="Usuario" asp-action="Create">Crear Usuario</a>
    </article>
</section>

@* 
﻿@{
    ViewData["Title"] = "Home Page";
}
@using PruebaMVC.ViewModels
@model IndexProductoViewModel

<h2>Product List</h2>

<table class="table table-dark">
    <thead>
        <tr>
            <th hidden>ID</th>
            <th>Nombre</th>
            <th>Precio</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var producto in Model.ProductoViewModels)
        {
            <tr>
                <td hidden>@producto.Id</td>
                <td>@producto.Nombre</td>
                <td>@producto.Precio</td>
                <td>
                    <a asp-controller="Producto" asp-action="EditarProducto" asp-route-idProducto="@producto.Id">Editar</a>
                    <a asp-controller="Producto" asp-action="DeleteProducto" asp-route-idProducto="@producto.Id" onclick="return confirm('¿Estas seguro que quieres eliminar el elemento?')">Eliminar</a>                      
                </td>
            </tr>
        }
    </tbody>
</table>

 *@